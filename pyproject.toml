[build-system]
requires = [
    "setuptools>=75.0.0",
    "wheel",
    "cython>=3.0.0",
    "numpy>=2.0.0",  # Must match your runtime requirement
    "setuptools_scm>=6.2"  # If you use setuptools_scm
]
build-backend = "setuptools.build_meta"